version: '3.8'

services:
  backend:
    build: ./backend
    expose:
      - "8000"
    environment:
      - PYTHONPATH=/app
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build: ./frontend
    expose:
      - "80"
    depends_on:
      - backend
    restart: unless-stopped
    environment:
      - REACT_APP_API_URL

networks:
  default:
    driver: bridge